# Experiments

This folder contains reproducibility artifacts. The central index is
`experiment_log.csv`; individual runs can add config snapshots under `configs/`
and generated predictions under `../submissions/`.

Do not edit public scores after the fact. If a submission was incorrect or
invalid, append a note in a new row or update the matching config notes in Git.
